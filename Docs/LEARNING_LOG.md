# 学習ログ

コードを変更する前後に、自分の言葉で短く書きます。正解を書くことより、説明できない場所を見つけることが目的です。

## コミットごとの確認

| コミット | 追加された機能 | 自分の説明 | まだ分からない点 |
|---|---|---|---|
| `05b8b3e` | UnityとGitの土台 |  |  |
| `26f55f7` | 左右移動・ジャンプ |  |  |
| `26873c8` | 紐・残り長さ |  |  |
| `dbad352` | HUD・灰色ステージ |  |  |
| キーボード操作 | 照準の回転・Eで紐操作 |  |  |
| 振り子物理 | 接線方向の力・運動量の保持 |  |  |

## 変更1回につき答える質問

1. どの入力を受け取ったか
2. どの値が変わったか
3. その値を誰が使うか
4. Unityの物理処理は `Update` と `FixedUpdate` のどちらで行われたか
5. 元に戻すにはどのコミットまで戻ればよいか

## 最初の実験

- `PlayerMover.moveSpeed` を `7` から `9` にする
- `Rigidbody2D.gravityScale` を `2.8` から `3.5` にする
- `RopeResource.maximumLength` を `20` から `12` にする

一度に全部変えず、1項目ずつPlayして、違いを1行で記録します。

## 足場配置の実験：最初の振り子

### 仮説

紐を掛ける場所と着地点を分けると、接続方法だけでなく、振り子の勢いと解除タイミングも最初の区間で学べる。

### 今回変えたもの

- 開始地点の右上に、接続しやすい `First Hook` を置く
- その右側に、振り子で飛び移る `First Landing` を置く
- 物理値、操作、紐の最大長は変えない

### 3回プレイして記録する

1. 紐を掛ける操作は迷わなかったか：
2. 着地に勢いをつける必要があったか：
3. 早く／遅く離した失敗の理由が分かったか：
4. 面白かった瞬間：
5. 思っていたのと違った瞬間：
