# 学習ログ

コードを変更する前後に、自分の言葉で短く書きます。正解を書くことより、説明できない場所を見つけることが目的です。

## コミットごとの確認

| コミット | 追加された機能 | 自分の説明 | まだ分からない点 |
|---|---|---|---|
| `05b8b3e` | UnityとGitの土台 |  |  |
| `26f55f7` | 左右移動・ジャンプ |  |  |
| `26873c8` | 紐・残り長さ |  |  |
| `dbad352` | HUD・灰色ステージ |  |  |
| キーボード操作 | 照準の回転・Eで紐操作 |  |  |
| 振り子物理 | 接線方向の力・運動量の保持 |  |  |

## 変更1回につき答える質問

1. どの入力を受け取ったか
2. どの値が変わったか
3. その値を誰が使うか
4. Unityの物理処理は `Update` と `FixedUpdate` のどちらで行われたか
5. 元に戻すにはどのコミットまで戻ればよいか

## 最初の実験

- `PlayerMover.moveSpeed` を `7` から `9` にする
- `Rigidbody2D.gravityScale` を `2.8` から `3.5` にする
- `RopeResource.maximumLength` を `20` から `12` にする

一度に全部変えず、1項目ずつPlayして、違いを1行で記録します。

## 足場配置の実験：最初の振り子

### 仮説

紐を掛ける場所と着地点を分けると、接続方法だけでなく、振り子の勢いと解除タイミングも最初の区間で学べる。

### 今回変えたもの

- 開始地点の右上に、接続しやすい `First Hook` を置く
- その右側に、振り子で飛び移る `First Landing` を置く
- 物理値、操作、紐の最大長は変えない

### 3回プレイして記録する

1. 紐を掛ける操作は迷わなかったか：正直わかりにくかった。
2. 着地に勢いをつける必要があったか：勢いをつけすぎると、慣性でうまく着地できなかった。
3. 早く／遅く離した失敗の理由が分かったか：やっていることは簡単なので理解できた。
4. 面白かった瞬間：紐を引っ掛けた瞬間と、うまく着地できた瞬間。
5. 思っていたのと違った瞬間：最初より難易度が明らかに下がり、足場が3個ある意味もあまり感じなかった。

### 分かったこと

- 接続成功と着地成功には、それぞれ気持ちよさがある
- 勢いをつけすぎて着地点を越える失敗は、理由を理解しやすい
- 接続地点がほかの足場と同じ見た目だと、どこへ紐を掛けるか分かりにくい
- 足場を増やすだけでは選択にならず、それぞれに違う役割が必要

## 足場配置の実験：安全と挑戦を選ぶ

### 仮説

接続地点を見分けやすくし、同じ振り子から安全な着地点と難しい着地点を選べるようにすると、次にどう動くか考える瞬間が生まれる。

### 今回変えたもの

- 紐を掛けられる地点を黄色で示し、1つだけにする
- 低く広い `Safe Landing` と、高く狭い `Risky Landing` を置く
- 着地点には紐を掛けられないようにする
- 物理値、操作、紐の最大長は変えない

### プレイして記録する

1. 黄色を見て、紐を掛ける場所だと理解できたか：
2. 飛ぶ前に、安全な着地点と難しい着地点のどちらを狙うか考えたか：
3. 実際に両方へ着地できたか：
4. 難しい着地点を狙いたくなった理由、または狙いたくなかった理由：
5. 面白かった瞬間と思っていたのと違った瞬間：

### 1回目の結果

- 全体の難易度は上がった
- 勢いが足りず、`Risky Landing` には到達できなかった
- 到達できないため、安全と挑戦を選ぶ状態にはまだなっていない

### 次の小さな調整

- 黄色の `First Hook` だけを右へ1移動し、2つの着地点へ少し近づける
- `Safe Landing` と `Risky Landing` の位置や大きさは変えない
- 物理値、操作、紐の最大長は変えない
- 3個目が「不可能」から「難しいけれど届く」に変わるか確認する

### 2回目の結果

- 難易度は簡単すぎず、難しすぎないと感じた
- 現在の配置は、コア体験を確かめるプロトタイプとしてちょうどよい
- 接続地点を少し近づける調整で、足場配置の難易度が適切になった

### Q3について分かったこと

- 今回は最初から3個目への到達を目標にしていたため、2個目と3個目のどちらを選ぶかは考えなかった
- 3個目へ到達するために、振り子のどのタイミングで紐を離すかは考えた
- 「次の足場をどう取るか」という計画は、経路選択ではなく解除タイミングの判断として生まれた

## 紐の残量実験：少ない数字だけで惜しさが生まれるか

### 仮説

接続中の残量が少なく見えると、紐を大切にしたい気持ちや「惜しい」という感情が生まれる。

### 今回変えたもの

- 紐の最大長と開始時の長さを `20` から `7` にする
- 操作、振り子の物理、接続地点、着地点は変えない
- 切断、残した紐の足場化、回復アイテムは追加しない

### プレイして記録する

1. 接続中の少ない残量が気になったか：現在の段階では評価しない。
2. 「あと少し紐があれば」と感じた瞬間があったか：現在の段階では評価しない。
3. 残量を見て操作や狙いを変えたか：現在の段階では評価しない。
4. 数字が少ないことで面白さが増えたか：現在の段階では評価しない。

### 今回の判断

- 紐を恒久的に消費する仕組みがない状態では、Q2の「惜しい」を考える段階ではない
- Q2は `No` と結論づけず、消費して進路を考える仕組みを試す段階まで保留する
- 最大長を小さくして数字だけを減らすことと、有限の身体を消費する面白さは分けて考える
- Q2比較用の仮値 `7` は基準値にせず、最大長と開始残量を `20` に戻す
- 今後の紐の総量は、メトリクス測定と消費方法の検証後に決める

## 操作不能の修正：地面の角への引っ掛かり

### 起きたこと

- 開始地面の右端で、プレイヤーの左下角と地面の右上角が接触すると動けなくなった
- 発生時は紐に接続しておらず、残量も最大だった

### 今回変えたもの

- プレイヤーの `BoxCollider2D.edgeRadius` を `0` から `0.08` にする
- 四角い見た目は変えず、当たり判定の角だけを少し丸める
- 移動、空中制御、振り子物理、紐の長さは変えない

### 確認すること

- 開始地面の右端で同じ姿勢になっても、左右移動やジャンプで抜けられるか
- 通常の着地や振り子の感触が変わっていないか

### 1回目の結果

- `edgeRadius` を `0.08` にしただけでは、別の足場の右上角でも動けなくなった
- 角に触れた状態では接地判定から外れ、空中移動の力より接触摩擦が強くなっていた

### 2回目の修正

- プレイヤー専用の物理マテリアルを実行時に作り、摩擦を `0` にする
- 角に触れたとき、空中移動の力で入力方向へ抜けられるようにする
- 移動速度、空中制御の力、振り子物理、紐の長さは変えない

### 2回目の結果

- 角には引っ掛かりにくくなったが、摩擦がないため足場の上で滑る感覚が生まれた

### 3回目の調整

- 摩擦だけを `0` から `0.1` にする
- 角から抜けられる低摩擦と、足場上で止まりやすい感触の両立を確認する
- ほかの物理値は変えない

### 3回目の結果

- 角への引っ掛かりと、足場上で滑る感覚の両方が改善した
- 摩擦 `0.1` を現在の基準値として採用する

## 次の実験に向けた測定基準

- 固定編みポイントの実験と現在の振り子を比較するため、`Docs/METRICS.md` に現在の設定値と配置を記録した
- プレイ結果は同じテストを5回行い、成功率、おおよその到達時間、失敗理由、解除タイミングを考えたかを記録する
- Q2は恒久的な紐の消費を実装するまで評価せず、現在の紐の最大長 `20` は仮の基準値として扱う

### テストAの結果

- 開始位置からFirst Hookへの接続は `5 / 5` 回成功した
- 照準や入力で迷うことはなく、現在の接続操作は問題なく理解できた

### テストBの結果

- First HookからSafe Landingへの着地は `3 / 5` 回成功した
- 振り子の動きが難しいと感じた
- 地上移動中のプレイヤーの動きが遅い一方で慣性が大きく、ストレスを感じた
- 摩擦 `0.1` は維持し、地上移動の最高速度と加速・減速の反応を次に切り分ける

## 地上最高速度の比較

### 仮説

地上の最高速度だけを少し上げると、現在の止まりやすさを大きく変えずに移動の遅さを改善できる。

### 今回変えたもの

- 地上の最高速度を `7` から `8` にする
- 加速度 `45`、摩擦 `0.1`、空中操作、振り子物理は変えない

### 確認すること

- 十分に速度が乗った後の地上移動が遅く感じないか
- キーを離したときの止まりにくさが強くなっていないか

### 追加調整

- `8` から、もう一段階速い `9` にする
- 加速度 `45`、摩擦 `0.1`、空中操作、振り子物理は引き続き変えない
- さらに最高速度 `12` を比較し、速すぎないかと停止時の感触を確認する
- 次に最高速度 `14` を比較し、地上操作と走りジャンプの飛距離を確認する

## 地上の滑りを短くする実験

### 仮説

最高速度と加速を変えず、キーを離したときの減速だけを強くすると、速さを保ったまま不自然な滑りを短くできる。

### 今回変えたもの

- 地上の加速と減速を別の値に分ける
- 加速は `45` のまま、キーを離したときの減速を `70` にする
- 最高速度 `9`、摩擦 `0.1`、空中操作、振り子物理は変えない

### 確認すること

- キーを離した後に滑っていく感覚が改善したか
- 止まり方が急すぎて不自然になっていないか

### 1回目の結果と追加調整

- 減速 `70` でも、氷の上を滑っているような感覚が残った
- 物理摩擦 `0.1` は角への引っ掛かり対策として維持する
- キーを離したときの減速だけを `70` から `120` にする
- この減速比較では最高速度 `9`、加速 `45`、空中操作、振り子物理は変えなかった

## 静止中でもジャンプできない問題

### 起きたこと

- 地上最高速度を比較している途中、開始地点で静止してSpaceを押してもジャンプできなかった
- Input ManagerのJumpはSpaceに設定され、ジャンプ力も `10` のままだった
- Unityログに `PlayerMover` の例外はなかった

### 原因と修正

- ジャンプ条件を満たすための接地判定が地面を拾えていなかった
- 下向きのBoxCastを、プレイヤーの足元に置く細いOverlapBoxへ変更する
- プレイヤー自身は判定から除外し、足場との重なりだけを接地として扱う
- 地上最高速度 `14`、加速 `45`、減速 `120`、摩擦 `0.1` は変えない

### 確認すること

- 開始地点で静止したままSpaceを押してジャンプできるか
- 地上を走りながらジャンプできるか
- 足場の横に触れただけで接地扱いにならないか

### 結果

- 開始地点でジャンプできるようになった
- 足元のOverlapBoxによる接地判定を維持する

## 地上最高速度の採用値

- 最高速度 `14` はプレイヤーが速すぎると感じた
- 比較した値の中から、地上最高速度を `8` に戻す
- 加速 `45`、減速 `120`、摩擦 `0.1`、空中操作、振り子物理は変えない

## 走りジャンプの横距離を分ける実験

### 仮説

地上速度を下げず、踏み切り時の横速度だけを少し減らすと、地上移動の速さとジャンプの扱いやすさを両立できる。

### 今回変えたもの

- 地上最高速度は `8` のままにする
- ジャンプした瞬間に横速度を75%残す
- 最高速で走っている場合、踏み切り直後の横速度は約 `6` になる
- 縦のジャンプ力 `10`、空中操作、振り子物理は変えない

### 確認すること

- ジャンプの高さが変わっていないか
- 走りジャンプの横距離が扱いやすくなったか
- 空中での左右操作が残っているか

### 追加調整

- 地上最高速度を `8` から `7` にする
- ジャンプ時の横速度保持率を75%から約64.3%にする
- 最高速からの踏み切り直後の横速度を約 `7` から約 `4.5` にする
- 加速 `45`、減速 `120`、ジャンプ力 `10`、摩擦 `0.1` は変えない

### 採用値

- 地上最高速度は `6.5` が完璧だと感じた
- ジャンプ直後の横速度約 `4.5` を維持するため、保持率を約69.2%にする
- 加速 `45`、減速 `120`、ジャンプ力 `10`、摩擦 `0.1` は引き続き変えない

## 連続ジャンプ測定用に開始地面を広げる

### 目的

連続してジャンプしたときの横移動距離を、同じ地面の上で確認できるようにする。

### 今回変えたもの

- 開始地面の右端 `x = 2.5` は動かさず、幅を `8` から `18` にする
- 開始地面の左端を `x = -15.5` まで延ばす
- 全体を見渡せるように、カメラ中心を `x = 2` から `x = -4.5` に移す
- カメラの正投影サイズを `6.2` から `6.5` にする
- First Hook、Safe Landing、Risky Landing、プレイヤー開始位置は変えない

### 確認すること

- 左側の長い地面で連続ジャンプを2〜3回確認できるか
- 従来の振り子区間の位置関係が変わっていないか
- 画面の縮小が操作感の確認を妨げないか

## プレイヤー操作の基準確定

- 地上最高速度 `6.5`、加速 `45`、減速 `120` を採用する
- ジャンプ力 `10`、最高速からのジャンプ直後の横速度約 `4.5` を採用する
- 物理摩擦 `0.1` と足元のOverlapBoxによる接地判定を採用する
- ここまでのプレイヤーの動作は納得できる形になった
- ステージ構想が固まるまでは、この組み合わせを基準として維持する

## 調整後のSafe Landing再測定

- 現在のプレイヤー操作で、Safe Landingへの着地は `4 / 5` 回成功した
- 調整前の `3 / 5` 回から成功率が上がった
- 現在の操作はこのままでよいと感じた
- ステージの構想によって必要な移動距離や難易度が変わるため、数値の追加調整は一旦止める
- 次はステージ構想を考え、必要になった段階で数値を再評価する

## 提案1・案B：通常解除時の一部返却実験

### 仮説

紐を離すたびに使用量の一部が永久消費されると、現在の振り子の試行錯誤を残しながら、残量を見て掛け方を考えるようになる。

### 今回変えたもの

- 通常解除時の返却率を `70%` にする
- 接続時に使った長さの `30%` は、解除しても戻らない
- HUDの接続中表示に返却率を出す
- 落下して開始地点へ戻った場合は、従来どおり残量を最大まで回復する
- プレイヤー操作、振り子の物理、足場配置は変えない

### プレイして確認すること

1. 紐の残量を以前より意識したか
2. 無駄な掛け直しを避けようと考えたか
3. 30%の消費を理不尽またはストレスに感じたか

この段階では返却率を最終決定せず、提案2の「切り離して足場を残す」を追加する前の単独実験として扱う。

### 消費量を決める時期

- 返却率 `70%` はステージ検証を始めるための仮値とし、現時点では確定しない
- 「使用距離に比例して消費し、通常解除では一部だけ戻る」というルールを先に固定する
- 仮値のまま、紐を使うか温存するか迷う灰箱ステージを作る
- 基本ルートを遊べる段階で、クリア可能性、立て直しやすさ、残量不足の納得感を確認する
- その結果から、紐の総量と返却率を一緒に調整してからステージを仕上げる

ステージを確定した数値へ無理に合わせることと、資源を無視したステージを完成後に直すことの両方を避ける。

## 次の灰箱ステージは計画難易度型にする

### 採用しなかった案

「安全だが接続回数が多いルート」と「難しいが接続回数が少ないルート」の比較は、難しい操作に成功できる人ほど資源を温存できる構造になる。これは判断より操作技術を問う実行難易度型へ寄るため、採用しない。

### 採用する方向

- どのルートも、接続と着地そのものは難しくしない
- ステージ全体とゴールまでの候補を、動く前に見渡せるようにする
- 近い足場を順番に使うと、個々の使用量は少なくても掛け直しが増え、総消費量が大きくなる
- 遠い足場を先に使うと、最初の使用量は多いが、その後の距離が短くなり、総消費量を抑えられる
- 最後の接続に必要な長さを残せなければ、ゴールへ届かない構造にする

### 検証したいこと

1. 動く前にルートや足場の順番を考えたか
2. 失敗理由を操作ミスではなく配分ミスとして理解できたか
3. 再挑戦時に足場を使う順番を変えたか
4. 解けたときに「うまく操作できた」より「分かった」と感じたか

次の実装では、足場を使う順番と総消費量を考える1画面の灰箱パズルを目指す。具体的な配置は、相談してから変更する。

## 接続前に投入する長さを選ぶ実験

### 気づいた問題

接続地点までの実距離を自動で消費量にすると、消費量はプレイヤーが考えて決めた配分ではなく、振り子のルートや操作結果で決まる。通常解除を全額返却に戻すだけでは「紐を使うことに代償がない」という元の問題へ戻る。

### 今回変えたもの

- 接続前に数字キー `1` / `2` / `3` で投入長 `4` / `8` / `12` を選ぶ
- 選んだ長さを接続可能距離と振り子の半径にする
- 接続に成功した場合だけ、選んだ長さを残量から引く
- 通常解除では選んだ長さの `70%` を返し、`30%` を永久消費する
- 届かない、または照準を外して接続できなかった場合は消費しない
- 足場配置、プレイヤー操作、振り子に加える力は変更しない

### 確認すること

1. 接続前に、どの長さを使うか考えたか
2. `4` では届かず、`8` または `12` なら届く違いを理解できたか
3. 長く選ぶことの移動上の利点と、永久消費の代償が結び付いて感じられたか
4. 消費を振り子操作の結果ではなく、自分で決めた配分だと感じたか

この実験を確認するまで、新しい計画難易度型の足場配置は作らない。

## 投入長を1単位ずつ決める実験

### 3段階選択の問題

`4` / `8` / `12` の3段階では現在のステージに対して選択が粗く、必要以上に長いヒモを使わされる。プレイヤーごとの「この長さで進む」という意志を細かく反映できない。

### 今回変えたもの

- 投入長の初期値と最低値を `1` にする
- 接続していない間に、`W`を1回押すと長さを1増やす
- `S`を1回押すと長さを1減らす
- 上限は現在の残量と射出上限 `14` のうち小さい方にする
- 接続解除後も選択値を維持し、残量を超えた場合だけ自動的に下げる
- 照準線を選択した長さまで伸ばし、接続前に届く範囲を確認できるようにする
- 接続中の長さ変更と、長押しによる連続変更はまだ追加しない

### 確認すること

1. First Hookへ必要だと思う長さを、照準線を見て自分で決められたか
2. 1単位ずつ押す操作を面倒に感じなかったか
3. 最低限の長さと余裕のある長さで、振り子の利点と消費の差を感じたか
4. 接続失敗が総当たりではなく、事前の見積もり違いだと理解できたか

### 1回目の結果

- 2段目の足場へ行く場合と3段目の足場へ行く場合で、必要なヒモの長さを変えて考える動作が生まれた
- 接続する前に目的地を見て「ここならこの長さ」と判断できた
- 1単位ずつ投入長を選べる仕組みは、とてもよいと感じた
- 消費量が振り子操作の結果だけで決まらず、プレイヤーが移動前に決める計画へ変わった
- 実際に選んだ数値は今回記録していないため、次回の測定項目にする

### 足場ごとの判断内容

- 2個目の足場を目指すときは、First Hookへ届く最低限の長さを選んだ
- 3個目の足場を目指すときは、最初に2個目で使った長さをそのまま試した
- その長さでは3個目へ届かなかったため、長さ不足だと判断した
- 次の試行では投入長を増やし、3個目へ到達できた
- 同じFirst Hookを使っても、目標の足場に応じて振り子半径を変える必要があると理解できた

この結果では、最初から3個目に必要な長さを予測したのではなく、失敗結果を読んで次の投入量を修正する計画が生まれている。

### 3個目へ到達したときの予測と修正

- 2個目へ移動したときの振り子の感覚を基準に、3個目へ必要な長さを自分で予想した
- 最初の予想では3個目へ届かなかった
- 失敗後に1ずつ総当たりせず、必要量を再予想して投入長を3増やした
- 3増やした長さで3個目へ到達できた

「前の結果を読む → 必要量を予想する → 試す → 失敗から予想を修正する → 成功する」という計画難易度型の学習ループを確認できた。

### 長さ8を固定した再現性テスト

- 3個目の足場へ到達できた投入長 `8` を固定し、5回試した
- 成功は `4 / 5` 回だった
- 失敗理由として、振り子操作の難しさと足場の小ささを感じた
- 成功するために、ヒモを解除するタイミングはかなり重要だった

投入長の計画を解いた後は高い確率で再現できたため、長さの判断は有効だった。一方で実行難易度も残っている。失敗した1回は解除タイミングが悪かったと理解でき、投入長の判断ミスとは混同しなかった。原因が明確なため、現時点では足場や物理を変更せず、「計画が主、解除タイミングが副」の基準として維持する。

## 1画面を縦に通すプロトタイプ

### 目的

振り子だけを確認する実験場から、開始・投入長の判断・移動・クリアまたは失敗・再挑戦までを一度通して遊べる短いゲームへ進める。

### 今回変えたもの

- 横長の1画面へステージを広げる
- 黄色い接続地点を3つ、幅 `4` の中間足場を3つ置く
- 右端に緑色のゴールを置く
- ゴールへ上から着地すると`CLEAR`にする
- 落下または残量1未満で`FAILED`にする
- 終了後に`R`を押すと位置・速度・残量を初期化する
- カメラは全体を見渡せる固定カメラのままにする

### 変えていないもの

- `W` / `S`による1単位の投入長選択
- 通常解除時の返却率 `70%`
- プレイヤーの移動、ジャンプ、振り子物理
- 敵、回復アイテム、複数ステージ、完成用アートは追加しない

### 確認すること

1. 最初からゴールまで止まらず遊べるか
2. 3区間で投入長を考える動作が繰り返し生まれるか
3. 広い足場によって実行難易度が支配的になっていないか
4. `CLEAR`と`FAILED`の理由が分かるか
5. `R`ですぐ再挑戦できるか

### 1回目の通しプレイ結果

- Start Groundから緑色のGoalまで到達し、クリアできた
- 開始時の残量`20.0`に対し、クリア時の残量は`15.2`だった
- 1面を通した実質消費量は`4.8`で、開始量の`24%`を使った
- 返却率`70%`から逆算すると、成功した接続で使った長さの合計は約`16.0`に相当する
- 今回は各区間で選んだ投入長と選択理由を記録していないため、残量`15.2`だけで総量や返却率は変更しない
- プレイ中は上手く着地点へ到達することだけを考え、ヒモの節約はまったく意識しなかった
- ヒモが減っていることは分かっても十分余ると感じたことと、残量表示自体をほとんど見なかったことの両方があった

通しでクリアできることは確認できたが、現在の1面では計画の中心が着地の実行にあり、ヒモ資源は判断材料になっていない。資源の余裕と情報の見落としが同時に起きているため、総量と表示を一度に変えず、片方ずつ比較する。

## 開始残量12の比較実験

### 仮説

開始残量`20`では、クリア時に`15.2`残り、節約も残量表示も意識しなかった。開始残量だけを`12`へ下げると、次の接続に必要な長さを残すため、残量が自然に計画へ入る。

### 今回変えたもの

- `RopeResource`の最大値と開始残量を`20`から`12`にする
- 通常解除時の返却率`70%`、HUD、足場配置、操作、物理は変えない
- 前回と同じ実質消費量`4.8`なら、クリア時には計算上`7.2`残る

### 確認すること

1. 指示されなくても残量表示を見たか
2. 次の接続分を残すために投入長を変えたか
3. 着地だけでなく、残量を含めて成功方法を考えたか
4. クリア時または失敗時の残量はいくつだったか

この実験では表示を変えない。開始残量`12`でも表示を見なければ、次に数字ではなくヒモそのものの見た目で残量を伝える方法を検討する。

### 1回目の結果

- 最初の足場へ到達するため、投入長`6`を選んだ
- その後は投入長を変えず、同じ長さ`6`ですべての足場へ到達できた
- 3個目の足場を経由しなくてもゴールへ到達できた
- 結果として、区間ごとに必要量を予測したり、残量に応じて投入長を変えたりする判断は生まれなかった

この結果は開始残量`12`の問題だけではない。投入長`6`が万能解になり、3個目の足場を省略できる現在の配置では、ヒモの配分を考える必要がない。総量やHUDを再調整する前に、各区間で異なる長さを選ぶ理由と、3個目の足場を通る意味を配置で作る必要がある。

### 6・4・8配置の比較準備

- 1区間目は投入長`6`、2区間目は`4`、3区間目は`8`を目安に配置する
- 3個目の着地点自体を緑色のゴールにし、省略できる別のゴール足場をなくす
- 開始残量`12`、返却率`70%`、採用済みの振り子物理は変えない
- まず各区間が想定した長さで到達可能かを確認し、総量の調整はその後に行う

この配置では、数値を強制する判定や順番ロックを追加しない。距離と高さだけで異なる投入長を選ぶ理由を作り、3個目を通る意味は「そこがゴールであること」で明確にする。

### 6・4・8配置の1回目

- 1区間目は投入長`6`を想定していたが、Hook 1へヒモが届かなかった
- 投入長を`8`へ増やすと接続できた
- この失敗は振り子操作や解除タイミングではなく、接続前の射出距離によるものだった
- 1区間目を`6`にする目的に対して、現在のHook 1 `(-3.8, 1.3)`は遠すぎる
- 次の変更ではLanding 1や採用済みの振り子物理を動かさず、Hook 1の位置だけを調整する

### Hook 1だけを近づける

- Hook 1を`(-3.8, 1.3)`から`(-5.0, -0.2)`へ移動する
- Landing 1、開始位置、振り子物理、ヒモ残量は変えない
- 投入長`6`で接続できることと、`5`では届かないことを比較する
- `6`で接続後にLanding 1へ着地できるかも確認する

### 3区間目の1回目

- Hook 3へヒモを掛けるには投入長`9`が必要だった
- しかし、長さ`9`の振り子半径では緑色のGoal / Landing 3へ到達できなかった
- 3区間目の想定は投入長`8`なので、現在のHook 3 `(14.5, 5.3)`は高く遠すぎる
- ゴール位置や採用済みの振り子物理を変える前に、Hook 3だけをLanding 2側へ近づける必要がある

### Hook 3だけを近づける

- Hook 3を`(14.5, 5.3)`から`(13.8, 4.3)`へ移動する
- Goal / Landing 3、Landing 2、振り子物理、ヒモ残量は変えない
- 投入長`8`で接続できることと、`7`では届かないことを比較する
- `8`で接続後にGoal / Landing 3へ着地できるかも確認する

### Hook 3とゴールの微調整

- Hook 3を`(13.8, 4.3)`から`(13.8, 3.9)`へ`0.4`下げる
- Goal / Landing 3を`(16.0, -0.5)`から`(16.6, -0.5)`へ`0.6`右へ動かす
- 振り子物理、投入長、足場サイズ、Landing 2は変えない
- 長さ`8`での接続しやすさと、右方向へ飛び出してゴールへ着地する余地を比較する

### ゴールをさらに右へ動かす

- Goal / Landing 3だけを`(16.6, -0.5)`から`(17.6, -0.5)`へ`1.0`右へ動かす
- Hook 3、振り子物理、投入長、ゴールの大きさは変えない
- 長さ`8`の振り子から右へ飛び出す必要性と、着地できる余地を比較する
- 画面右端に近づくため、ゴールの見切れが気になる場合は位置を戻さずカメラ側を別に検討する

### 微調整後のクリア結果

- Hook 3 `(13.8, 3.9)`、Goal / Landing 3 `(17.6, -0.5)`の配置でクリアできた
- ゴールにはぎりぎり到達する感覚だった
- クリア時の残量は`6.9 / 12.0`だった
- 最終区間は到達不可能な状態から、到達限界に近い成功可能な状態へ変わった
- この時点では配置をさらに動かさず、ぎりぎり感が面白さかストレスか、同じ方法で再現できるかを確認する

### ぎりぎり感の評価と実測投入長

- 振り子をしている間は楽しいと感じた
- 着地するときは、届くか届かないかの緊張感があった
- 各区間で使った投入長は`6 → 3 → 7`だった
- 想定した`6 → 4 → 8`より2区間目と3区間目が1ずつ短かったが、区間ごとに異なる長さを使う形にはなった
- 現在の配置は、振り子の楽しさと最終着地の緊張感を両立しているため、すぐには動かさない

投入長`6 + 3 + 7 = 16`を各1回だけ使い、通常解除で30%ずつ消費した場合、理論上の残量は`7.2`になる。画面では`6.9`だったため、長さ`1`相当の追加接続があったか、次の試行で確認する。

### ゴールを0.5右へ微調整する

- Goal / Landing 3だけを`(17.6, -0.5)`から`(18.1, -0.5)`へ動かす
- Hook 3、振り子物理、ヒモ残量、ゴールの大きさは変えない
- 振り子の楽しさと着地時の緊張感を残しながら、到達可能性を再確認する
- プレイ確認では、この位置で問題なくクリアできた
- 体感評価は「完璧」で、ぎりぎりの緊張感と到達可能性のバランスが取れた
- Goal / Landing 3の位置`(18.1, -0.5)`を現在の採用値とし、追加調整は行わない

## 振り子を放置したときの自然減衰

### 気づいたこと

接続中の物理減衰は`0.02`だったため、入力をやめても勢いがほとんど弱まらず、放置した振り子として不自然に見えた。

### 今回変えたもの

- 接続中の`Rigidbody2D.linearDamping`を`0.02`から比較用の仮値`0.12`へ上げる
- 速度を直接減らす処理は追加しない
- 重力と`DistanceJoint2D`の張力で振り子運動を作り、Unityの物理抵抗で徐々にエネルギーを失わせる
- A/D入力は従来どおり円の接線方向へ力を加える
- 解除時は従来どおり、その瞬間の速度を保存する

### 確認すること

1. A/Dを離して見ていると、振幅と速度が少しずつ弱まるか
2. すぐ止まらず、振り子らしい往復が十分続くか
3. A/Dを再入力すると、自分で勢いを戻せるか
4. 解除した瞬間に不自然な減速や加速が起きないか

### 1回目の体感

- A/Dで力を加えたときの勢いの付き方が不自然に感じた
- 現在はキーを押している間、一定の接線方向の力`18`を毎物理フレーム加えている
- この方式は振り子を揺らす操作より、接線方向へモーターで加速する感覚になりやすい
- 数値や方式を変更する前に、押した瞬間が強いのか、押し続けると強いのか、振り子の向きと力が合わないのかを切り分ける

## A/D入力だけで一回転できる問題

### 起きたこと

- A/Dで加速できること自体はよい
- ただし、入力を続けると一回転できるほど勢いが付き、振り子として不自然だった

### 今回変えたもの

- 現在位置の高さと接線速度から、振り子の単位質量あたりの力学的エネルギーを求める
- 振り角約`80度`へ到達するエネルギーを、A/Dが追加できる上限の目安にする
- 上限の75%から接線方向の入力を滑らかに弱め、上限では追加の加速を止める
- 上限を超えていても、反対方向へ入力して減速する操作は妨げない
- 速度の直接制限や位置の強制移動は行わず、重力、`DistanceJoint2D`、物理減衰で運動を続ける

### 確認すること

1. A/Dで着地に必要な勢いを十分作れるか
2. A/Dを押し続けても一回転しないか
3. 約80度付近で、加速が急に切れたように感じないか
4. 反対方向の入力で勢いを弱められるか
5. Eを離した瞬間の飛び出しが従来どおり自然か

### 上限角度の追加調整

- 一回転は防ぎながら、もうほんの少し上まで行ける余地が欲しいと感じた
- 入力で追加できるエネルギーの上限角度を`80度`から`85度`へ上げる
- 水平になる`90度`未満に留め、入力だけで一回転できない設計は維持する
- 加速力`18`、物理減衰`0.12`、上限へ近づくと滑らかに入力を弱める方式は変えない

### 採用結果

- 上限角度`85度`でプレイし、振り子は完璧だと感じた
- 加速力`18`、最大速度目安`15`、物理減衰`0.12`、入力エネルギー上限角度`85度`を現在の基準値として採用する
- 入力による加速、放置時の自然減衰、一回転防止、高さの余裕を両立できた
- ステージや資源設計の検証で明確な問題が出るまで、振り子の物理値は変更しない

## 最低限の投入長を選ぶプレイ

### 2026-08-10：計画判断と実行失敗の切り分け

- 各区間では、ヒモをHookへ引っかけられる最低限の長さを選んだ
- 長さの判断は、余裕を持たせるのではなく、接続成立に必要な量を見積もる方針だった
- 主な失敗理由は振り子を離すタイミングが悪かったことだった
- クリア時のヒモ残量は`5.1 / 12.0`だった
- 今回は長さ選択という計画判断が成立した一方、失敗は解除タイミングという実行面で起きた
- 次は各区間で最低限より`1`長く選び、残量・成功しやすさ・判断内容を比較する

### 2026-08-10：最低限より1長く選んだ結果

- 現在のステージでは、Hookへ届く最低限の長さがゴールへ向かう振り子半径としても最適だった
- 最低限より`1`長くすると振り子の半径と軌道が変わり、ゴールするのが非常に難しくなった
- 画像ではクリアできており、その時のヒモ残量は`6.0 / 12.0`だった
- 「長く使えば安全」という関係ではなく、投入長が移動軌道そのものを決めることが確認できた
- 一方、現配置では「届く最低限」が資源面と移動面の両方で最適なため、長さ選択の最適解が一択になっている
- 次の配置変更は行わず、複数の投入長に異なる利点を持たせるかを先に検討する

## 先行投資を試す計画分岐

### 2026-08-10：上ルートを追加する

- 既存の下ルートとゴール位置は維持する
- Hook 2から長めのヒモで到達する上側の`Planning Landing`を`(9.5, 0.8)`に追加する
- 上側から狙いやすい`Planning Hook`を`(14.8, 3.0)`に追加する
- 下ルートは今の投入を抑える代わりに、従来どおり最後に長いヒモが必要になる想定
- 上ルートはHook 2で先に多く使う代わりに、次のHookまでの距離を短くする想定
- 足場幅は既存のLanding 2と同じ`4.0`にし、極端な着地難度差を作らない
- 目的は、最短長が常に最適になる状態から「今使って後を短くする」という計画判断を作ること
- 位置と想定投入長は仮値とし、実際に両ルートでクリアできるかを先に確認する

### 初回プレイで判明した問題と修正

- Hook 2へ投入長`6`で接続できたが、振り子の入力上限`85度`では上側の`Planning Landing`へ到達できなかった
- プレイヤー操作ではなく、足場の高さ`0.8`が振り子設定に対して高すぎる配置問題と判断した
- 確定済みの振り子物理は変えず、`Planning Landing`を`(9.5, 0.8)`から`(9.5, -0.4)`へ下げる
- 落下時は`FAILED`地点で物理が停止し、`R`を待つため、リスポーンせず固まったように見えた
- 落下時だけ`FAILED`を`0.5`秒表示して自動復帰するようにし、`R`による手動復帰も維持する
- 残量が`1`未満になった場合は、自動復帰せず従来どおり`R`を待つ

### 新旧ルートの初回残量比較

- 既存の下ルートでクリアしたときの残量は`6.9 / 12.0`だった
- 追加した計画ルートでクリアしたときの残量は`7.2 / 12.0`だった
- 初回比較では、計画ルートの方が`0.3`多くヒモを残せた
- 「途中で先に使い、後の接続を短くする」ルートが総消費を抑える可能性を確認できた
- ただし再接続回数でも残量が変わるため、今回だけで計画ルートが常に得とは確定しない
- 次に計画ルートで各Hookへ投入した長さと再接続回数を記録し、残量差の理由を確認する

### 投入長を確認した結果

- 計画ルートで選んだ長さは`6 → 6 → 4`、失敗回数は`1`回だった
- 合計投入長は`16`で、既存ルートの`6 → 3 → 7`も合計`16`になる
- 返却率70%で追加接続がなければ、どちらも理論残量は`12 - 16 × 0.3 = 7.2`
- 既存ルートの実測`6.9`との差は、以前から保留していた長さ`1`相当の追加接続で説明できる
- そのため「計画ルートの方が総消費を抑えた」という初回判断は訂正する
- 現在の分岐は、途中で`3`多く使って最後に`3`少なくするが、合計消費は同じ構造になっている
- 計画判断を成立させるには、消費総量以外に「途中で必要な残量」や「その後の選択肢」に違いが生まれたかを確認する必要がある

### 新ルートを選んだ理由

- 新ルートを選んだ理由は「新しく追加されたルートだから試した」だった
- 「中盤で長さ`6`を使えば、最後を`4`にできる」という先行投資を考えて選んだわけではなかった
- 初回プレイでは新規性が選択理由になり、狙っていた資源計画の判断は確認できなかった
- 開始残量`12`では既存ルートと新ルートの両方に十分な余裕があり、投入順序の違いが制約になっていない
- 次の候補は、配置と消費率を変えず、開始残量だけを一時的に下げて投入順序に意味が生まれるか試すこと

## 開始残量9の投入順序実験

### 2026-08-10：先行投資が必要になる条件を作る

- 開始残量と最大値だけを`12`から比較用の仮値`9`へ下げる
- 返却率70%、振り子物理、ステージ配置、各区間の必要長は変えない
- 最初に長さ`6`を使った後は`7.2`残る
- 既存ルートで次に`3`を使うと`6.3`になり、最後に必要な`7`を使えない
- 計画ルートで次に`6`を使うと`5.4`になり、最後に必要な`4`を使える
- 「今は短い方が得」という局所判断ではなく、後で必要な長さを見て投入順序を選ぶか確認する
- この`9`は完成版の採用値ではなく、計画判断が生まれるかだけを見る一時的な実験値

### 実験を一旦保留する

- 計画分岐の機能実験は一旦飛ばすことにした
- 開始残量と最大値を実験値`9`から、安定していた基準値`12`へ戻す
- 計画分岐の足場とHookは削除せず、比較結果とともにGit履歴へ残す
- 再開時は、投入順序を制約にする方法が本当に必要かから改めて相談する

## 横方向だけのカメラ追従

### 2026-08-10：固定カメラとの比較版

- プレイヤーのX座標だけを`0.18`秒の滑らかさで追従する
- カメラのY座標`0`、Z座標`-10`、Orthographic Size`8.7`は変えない
- 振り子の上下動ではカメラを動かさず、画面の縦揺れを防ぐ
- 先読み、ズーム、画面揺れ、ステージ端の制限は追加しない
- 独立した`HorizontalCameraFollow`として追加し、固定カメラへ戻しやすくする
- 確認すること：操作しやすくなったか、次のHookや足場を考える視野が狭くなりすぎないか

### 2026-08-11：比較結果と採用判断

- 横方向だけの追従カメラでプレイし、体感評価は「めちゃくちゃいい感じ」だった
- 操作や視野について新しい問題は報告されず、固定カメラより横追従を採用することにした
- X方向の追従時間`0.18`秒、Y座標`0`、Z座標`-10`、Orthographic Size`8.7`を現在の基準値とする
- ステージや導入の検証で明確な問題が出るまで、追従速度や画角を追加調整しない

### 今後の候補：ゴールから開始地点へ戻るステージプレビュー

- ステージ開始時に、カメラをゴール地点から開始地点へ移動させる案が出た
- 足場、Hook、ギミック、ゴールまでの構成を操作前に見せ、「どう進むか」を考える材料にする
- ヒモヒトの計画難易度と相性がよく、単なる演出ではなく導入と情報提示の役割を持つ
- 実装前に、移動時間、スキップ入力、プレビュー中の操作停止、リトライ時にも再生するかを相談して決める
- 現時点では候補として記録するだけで、横追従カメラの採用と`main`への統合を先に行う

## 1週間作業指示書：CP1の完了確認

### 2026-08-11：`main`統合後の通し確認

- 実験ブランチの75コミットを、履歴を書き換えない早送りで`main`へ統合した
- 統合後の`main`をGitHubへ送信し、ローカルと`origin/main`が一致していることを確認した
- Unityで開始地点からゴールまでプレイし、`CLEAR`まで到達できた
- `R`を押すと、位置、速度、ヒモ残量が初期状態へ戻った
- 足場から落下すると、`FAILED`の表示後に自動で開始地点へ戻った
- 横方向だけの追従カメラも通しプレイ中に正常に動作した
- 以上により、作業指示書のCP1「main取り込みと動作確認」は完了とする

## CP2：起動時の操作説明画面

### 2026-08-11：文字だけの比較版

#### 仮説

ゲーム開始前に操作を1画面で示し、`W`/`S`による投入長選択を他の操作より目立たせると、初見でも本作の中心操作に気づける。

#### 今回変えたもの

- 起動時に、目的と操作をまとめた文字だけの開始画面を表示する
- 「`W` / `S`で次に使うヒモの長さを選ぶ」を黄色で強調する
- 開始画面中はプレイヤーの物理、移動、ヒモ操作を停止する
- マウスでGame画面を選んだだけでは開始せず、キーボードのキーを押して離すと開始する
- `R`による再挑戦と落下時の自動復帰では、開始画面を再表示しない
- Canvas、画像、完成用アートは追加せず、既存の`OnGUI` HUDを利用する

#### 確認すること

1. Play直後に操作説明画面が表示されるか
2. 説明中に`A`、`D`、`W`、`S`、`E`を押してもプレイヤーやヒモが動かないか
3. `W`/`S`の説明が最も重要な操作として目に入るか
4. キーを押して離すと説明が消え、通常どおり操作できるか
5. `R`と落下時の自動復帰で、説明画面に戻らずすぐ再挑戦できるか

### 2026-08-11：プレイ中HUDの日本語化

#### 分かったこと

- 開始画面からプレイへ移った後、左上HUDが英語のままだったため理解が途切れた
- `W: +1 / S: -1`が1行にまとまっており、それぞれの役割が直感的に分かりにくかった

#### 今回変えたもの

- `ROPE`、`LENGTH`、`SPEED`、状態表示、操作案内を日本語へ変更する
- `W`は「使う長さを1増やす」、`S`は「使う長さを1減らす」と別々の行に表示する
- 開始画面の`W`/`S`も同じ文言へ統一する
- 行数が増えたため、左上HUDの高さを`204`から`286`へ広げる

#### 確認すること

1. プレイ開始後、ヒモ残量と次に使う長さの違いが分かるか
2. `W`で増え、`S`で減ることが一目で分かるか
3. 日本語化でHUDが枠からはみ出していないか

#### 1回目の表示結果

- ヒモ残量、次に使う長さ、`W`と`S`の役割は日本語で別々に表示できた
- 文字サイズは読みやすかったため変更しない
- HUD下部の「ヒモ」「再挑戦」「マウス照準」が枠から見切れていた
- 内容を削らず、HUDの高さだけを`286`から`340`へ広げて再確認する

#### 2回目の表示結果

- HUD下部まで枠内に収まり、表示は「完璧」と評価した
- 日本語の文字サイズ、`W`と`S`を分けた案内、HUD高さ`340`を現在の採用値とする
- プレイ画面の構成変更で明確な問題が出るまで、左上HUDの大きさを追加調整しない

### 2026-08-11：最初の区間で長さを選ぶ練習

#### 仮説

Hook 1へ届く最低限の長さと、長すぎて振り子が低くなる長さを同じ場所で比較できれば、`W`/`S`が単なる設定ではなく、進み方を考える操作だと理解しやすくなる。

#### 今回変えたもの

- Start GroundとLanding 1の間に、落下を防ぐ`Practice Safety Floor`を1つ追加する
- 練習床の中心を`(-4.5, -7.75)`、大きさを`4.0 × 0.7`とする
- 長さ`6`は床の上を通過し、長さ`7`は振り子が低くなって床に触れる配置を狙う
- フック、着地点、ヒモ資源、プレイヤー物理の設定は変えない

#### 確認すること

1. 長さ`5`ではHook 1へ届かないか
2. 長さ`6`ではHook 1へ接続でき、練習床に触れずLanding 1へ進めるか
3. 長さ`7`では練習床に触れ、長すぎることが動きから分かるか
4. 練習床に着地した後、ヒモを離して`W`/`S`で長さを選び直せるか
5. 練習床がLanding 1への意図しない近道になっていないか

#### 比較結果

開始地点から長さ`5`、`6`、`7`をそれぞれ使い、すべてLanding 1へ到達できた。

| 選んだ長さ | Landing 1到達後のヒモ残量 | 着地しやすさ | 解除タイミングの合わせやすさ |
| --- | --- | --- | --- |
| `5` | `10.5` | 3位 | 3位 |
| `6` | `10.2` | 1位 | 1位 |
| `7` | `9.9` | 2位 | 2位 |

#### 分かったこと

- 「長さ`6`だけが通過できる」という最初の仮説は再現せず、`5`、`6`、`7`のすべてが到達可能だった
- 長さ`5`は最も節約できるが、着地と解除タイミングが最も難しかった
- 長さ`6`は`5`より`0.3`多く消費する代わりに、着地と解除タイミングの両方が最も扱いやすかった
- 長さ`7`は`6`より`0.3`多く消費する一方、扱いやすさは`6`を上回らなかった
- 最短だけが正解ではなく、節約を優先するか、操作の余裕にヒモを使うかという選択が生まれた
- 練習床で特定の長さを不正解にするより、複数の長さを成功可能にして消費量と扱いやすさを比較する方が、計画難易度型という方針に合っている

#### 採用判断

- 「ヒモは長くすればよいわけではなく、場所に合う長さがある」とプレイヤーが体験から学べることには、追加する価値があると判断した
- 長さ`5`は節約、長さ`6`は操作の余裕、長さ`7`は長くしすぎると消費も操作性も不利になる例として扱う
- 最初の練習区間では長さ`7`へ別の利点を足さず、現在の`5`、`6`、`7`の関係を採用する
- この判断に伴う物理設定や配置の追加変更は行わない

### CP2の到達判断

- 起動時の操作説明画面で、中心操作である`W`/`S`を強調できた
- プレイ中HUDも日本語化し、`W`と`S`の役割を別々に表示できた
- 最初の区間で長さ`5`、`6`、`7`を比較し、節約、操作の余裕、長くしすぎる不利を確認できた
- 制作者本人による実装確認と内部プレイは完了したため、CP2の制作作業は完了扱いとする
- 完了条件にある「初見の人が説明なしで越えられるか」は、今後の第三者テストで別途確認する
- 次は指示書の推奨順に従い、CP3②「長さ選択が一択になっていないか」を5回の自由選択プレイで確認する

## CP3②：長さ選択が一択になっていないか

### 2026-08-11：自由選択で分かったこと

- 長いヒモを選んでも明確な地形上のデメリットが少ないため、失敗しにくそうな長いヒモを選んだ
- 現在のステージには「場所に適した長さを選ぶ」場面が少ないと感じた
- 長さ`5`と`7`の永久消費差は`0.6`だけであり、この差が後の進行を制限しない限り、安全を優先して長い方を選ぶのが合理的である
- この結果から、CP3②の完了条件である「長短のどちらにも利点があり、選択に迷う」はまだ満たしていないと判断する

### 安全床を1つの値だけ動かす実験

- `Practice Safety Floor`のY座標だけを`-7.75`から`-7.25`へ変更する
- Hook、Landing 1、振り子物理、返却率、開始残量は変更しない
- 長さ`5`は節約できるが難しい、長さ`6`は床を通過しやすい、長さ`7`は振り子が低くなって床に当たる状態を狙う
- 同じ開始地点から長さ`5`、`6`、`7`を比較し、安全床との接触、着地、解除タイミングを確認する

#### 衝突確認の結果

- 振り子中のプレイヤー本体が`Practice Safety Floor`の内部まで入り込む状態を、録画の一時停止画像で確認した
- ヒモの線だけではなく、水色のプレイヤー本体と灰色の安全床が重なっている
- プレイヤーと安全床には有効な`BoxCollider2D`があり、安全床はTriggerではない
- プレイヤーの`Rigidbody2D`は連続衝突判定で、接続中だけ衝突を無効にするコードもない
- したがって、床の高さだけを変えて「長いヒモを床に当てる」実験は、現在の衝突状態では正しく評価できない
- CP3②は引き続き未完了とし、床をさらに動かす前に、障害物の作り方を相談して決める

### 長い振り子の外周に縦長障害物を置く実験

- 横向きの安全床は落下防止用として残し、長さ判定には使わない
- Landing 1の右下に`Practice Long Rope Obstacle`を追加する
- 障害物の中心を`(1.5, -4.5)`、大きさを`0.6 × 2.0`とする
- 短い振り子は内側を通り、長い振り子だけが外側で障害物に触れる状態を狙う
- Hook、Landing 1、振り子物理、返却率、開始残量は変更しない
- 長さ`5`、`6`、`7`で、障害物への接触とLanding 1への着地を比較する

#### 縦長障害物の結果

- 長さ`7`の振り子は、狙いどおり縦長障害物の位置まで到達した
- しかし、接続中のプレイヤー本体は障害物との衝突で止まらず、長いヒモのデメリットとして機能しなかった
- 横向きの安全床と縦長障害物の両方で同じ結果になったため、障害物の向きや座標だけの問題ではない
- 指示書が想定した「障害物を1つ置くだけ」の方法は、現在の接続中の物理構成では成立しなかった
- 座標調整を続けず、障害物への接触を明示的に処理するか、衝突を使わない計画難易度へ切り替えるかを相談して決める

### ヒモ外し障害物の実験

#### 狙い

- 「長いヒモを選べば安全」という考えに対し、長い軌道だけが触れる邪魔ギミックを作る
- 障害物への接触でヒモが外れ、実際のステージでは落下からゲームオーバーへつながる構成を想定する
- 今回は実験のため安全床を残し、接触検知と自動解除だけを確認する

#### 今回変えたもの

- 縦長障害物を赤色へ変え、通常の足場と区別する
- 固い本体Colliderに加えて、同じ大きさのTriggerを追加する
- 接続中のプレイヤーがTriggerへ入ると、既存の通常解除処理を呼ぶ
- 自動解除でも勢いを維持し、投入長の70%を返す既存ルールは変更しない
- 障害物の位置と大きさ、Hook、Landing 1、振り子物理、開始残量は変更しない

#### 確認すること

1. 長さ`5`と`6`では自動解除されずLanding 1へ進めるか
2. 長さ`7`では赤い障害物への接触時に、`E`を押したままでもヒモが外れるか
3. 自動解除の瞬間に、それまでの勢いが不自然に消えないか
4. 自動解除後に安全床へ落ち、失敗理由を理解できるか
5. 自動解除後のヒモ残量が通常解除と同じ計算になっているか

#### 最初の接触検知で分かったこと

- `OnTriggerEnter2D`だけでは、接続中に赤い障害物へ触れてもヒモが外れなかった
- Trigger自体はシーンに存在し、スクリプトもコンパイルできていたため、一度きりの通知へ依存する方法をやめる
- `OnTriggerStay2D`に加え、物理更新ごとに障害物とプレイヤーのColliderの重なりを確認する
- 解除処理、勢いの維持、70%返却、障害物の位置は変更しない

#### 修正後の実験結果

- 長さ`5`では赤い障害物に当たらず、Landing 1へ到達できた
- 長さ`6`でも赤い障害物に当たらず、Landing 1へ到達できた
- 長さ`7`では赤い障害物に接触し、ヒモが自動で外れて安全床へ落下した
- 「長いほど安全」ではなく、必要以上に長いヒモには軌道上の危険がある状態を作れた
- 長さ選択が残量差だけでなく、通る軌道と障害物を予測する判断にもつながった
- 実験用の安全床では結果を確認でき、本番で安全床を外せば落下・ゲームオーバーへつなげられる

### 2026-08-11：CP3②の5回自由選択テスト

同じ開始地点からLanding 1までを5回プレイし、投入長は毎回指定せず自由に選んだ。

| 回 | 選んだ長さ | 選択理由・試したこと | 結果 |
| --- | --- | --- | --- |
| 1 | `7` | 安全に行けそうな`6`より長ければ、さらに安全だと予想した | 障害物に接触して失敗 |
| 2 | `5` | `7`で接触したため、2段階短くして安全策を取った | Landing 1へ到達 |
| 3 | `6` | `5`で安全に到達できた経験から、少し長くしても通れると予想した | 障害物をぎりぎり回避して到達。ハラハラ感があった |
| 4 | `7` | 照準を90度に近づけ、振り子の横幅を狭めれば回避できると予想した | 障害物に接触して失敗 |
| 5 | `6` | 3回目の成功と障害物との距離感を基に、照準をさらに90度へ近づけた | 接触の不安を減らして安全に到達 |

#### 判定

- 選択回数は`5`が1回、`6`が2回、`7`が2回で、5回中3種類の長さを自分の判断で選んだ
- 成功は`5`が1/1回、`6`が2/2回、`7`が0/2回だった
- `5`は障害物を避けやすく節約できるが、過去の比較では着地と解除タイミングが難しい
- `6`は`5`より消費する代わりに扱いやすく、照準角度によって危険との距離を調整できた
- `7`は「長いほど安全」という予想を裏切る危険な長さで、照準による回避も試したくなる対象になった
- 長さだけでなく、長さと照準角度を組み合わせて軌道を予想する行動が生まれた
- CP3②の判定条件である「5回中2回以上異なる投入長を選ぶ」を満たしたため、制作者本人による内部検証は完了とする
- 初見の第三者にも同じ判断が生まれるかは、今後の外部プレイテストで確認する

## 「編む」を固定足場にする最小実験

### 仮説

正常なヒモ解除を2回行い、その結果として固定足場を編めれば、「自分のヒモを消費して進む」と動詞の「編む」が一つの流れになる。

### 今回試す仕様

- プレイヤーが`E`を離して正常解除したときだけ、編み糸を1個獲得する
- 赤い障害物による強制解除、落下処理、終了処理では編み糸を獲得しない
- 編み糸を2個持って紫色の編み枠付近で`Q`を押すと、指定位置に固定足場が完成する
- 細かな消費量は計算せず、解除回数だけを使う
- 再挑戦時は編み糸を0個へ戻し、編んだ足場も未完成へ戻す
- 既存ゴール位置`(18.1, -0.5)`を編まれる足場にし、実験用ゴールを`(23.0, -0.5)`へ移す

### 確認すること

1. 自分でヒモを2回解除すると、編み糸が2個になるか
2. 赤い障害物による強制解除では、編み糸が増えないか
3. Landing 2付近の編み枠で`Q`を押すと、紫色の固定足場が現れるか
4. 素材が足りない場合に「あと何個必要か」が分かるか
5. 編んだ足場を使って右側のゴールへ到達できるか
6. 「使ったヒモが道になる」ことを面白いと感じるか

### 赤い障害物の対象Hookを限定

- 編む実験を追加した後、Landing 1からLanding 2へ移る際にも赤い障害物へ触れ、ヒモが外れる問題が見つかった
- 原因は、赤い障害物が「どのHookに接続しているか」を区別せず、接続中のすべての接触を強制解除していたこと
- `RopeController`が現在接続中の`HookPoint`を記録し、解除時に記録を消すようにした
- 赤い障害物は`Hook 1`へ接続している場合だけ強制解除する
- Hook 1での長さ`5`、`6`、`7`の比較実験は維持し、Hook 2以降の通常ルートには影響させない

#### 確認すること

1. Hook 1へ長さ`7`で接続し、赤い障害物へ触れるとヒモが外れるか
2. Hook 1へ長さ`5`または`6`で接続した既存ルートが変わっていないか
3. Landing 1からHook 2へ接続中に赤い障害物へ触れても、ヒモが外れないか
4. 赤い障害物による強制解除で編み糸が増えない既存ルールが維持されているか

### 編む実験用ゴールの微調整

- `Goal / Landing 3`だけを`(23.0, -0.5)`から`(23.15, -0.5)`へ`0.15`右に動かす
- 編まれる足場、編み枠、Hook、物理設定、ヒモ残量は変更しない
- 編んだ足場からゴールへ移る最後の距離感だけを確認する

#### プレイ確認結果

- ゴールを`0.15`右へ動かした状態で、最後の区間を問題なくクリアできた
- 制作者本人の評価は「完璧」だった
- `Goal / Landing 3`の位置`(23.15, -0.5)`を、編む実験の現在の採用値とする

### 編む場所と条件の伝わり方

- 「`Q`を押す場所と条件が分かりやすかったか」という確認に対し、制作者本人は分かりやすいとは感じなかった
- 現状では、紫色の編み枠が何をする場所なのか、編み糸が何個あれば使えるのかがプレイ中に十分伝わっていない
- この状態では、「編むこと自体が面白いか」と「操作方法が分からない」を切り分けて評価できない
- 次は編む仕様や消費量を増やす前に、場所と条件の伝え方だけを小さく比較する

### 編む場所の近接表示

- 紫色の編み枠へ近づいた時だけ、画面下中央に専用パネルを表示する
- パネルには「編む場所」「編み糸の現在数／必要数」「`Q`で足場を編む」または「あと何個必要」を表示する
- 左上HUD内にあった短い条件文は外し、場所と操作を一つの専用表示としてまとめる
- 編み糸の獲得条件、必要数`2`、`Q`操作、足場の位置は変更しない

#### 確認すること

1. 紫色の編み枠へ近づいた時だけ専用パネルが現れるか
2. そこが「編む場所」だと分かるか
3. 現在の編み糸数と必要数が分かるか
4. 足りる場合は`Q`、足りない場合は残り必要数が迷わず分かるか
5. パネルがプレイヤーや着地点を見る邪魔にならないか

### 近接表示をプレイヤーの頭上へ移動

- 画面下中央では、案内とプレイヤーのいる場所が離れて見えるため、専用パネルをプレイヤーの頭上へ変更する
- カメラの移動中もプレイヤーを追従し、画面端では表示が見切れない範囲に収める
- 表示内容、表示条件、編み糸の必要数、`Q`操作は変更しない
- 場所と操作が結び付きやすくなったか、プレイヤーや進行方向を隠さないかを確認する

### 編まれる橋とゴールを同時に右へ移動

- 編んで生成される橋を`(18.1, -0.5)`から`(19.1, -0.5)`へ移動する
- ゴールを`(23.15, -0.5)`から`(24.15, -0.5)`へ移動する
- 両方をX方向へ同じ`+1`だけ動かすため、橋とゴールの距離感は変更しない
- 編み枠、Hook、物理設定、ヒモ残量は変更しない

#### 追加調整

- 橋を`(19.1, -0.5)`から`(21.1, -0.5)`へ、ゴールを`(24.15, -0.5)`から`(26.15, -0.5)`へ移動する
- 両方をさらにX方向へ`+2`動かし、橋とゴールの間隔`5.05`は維持する

### 実験用の安全床と下側ルートを削除

- `Practice Safety Floor`を削除し、赤い障害物で強制解除された場合は落下して失敗にする
- 下側の`Landing 2 (8, -2)`を削除し、上側の`Planning Landing (9.5, -0.4)`を通る構成に絞る
- Hook、Planning Landing、編み枠、生成橋、ゴール、物理設定は変更しない
- 実験結果を受け止めるための床を外し、現在の短いステージをチュートリアルに近い一続きのルートへ整理する

#### 確認すること

1. 長さ`7`で赤い障害物に触れると、そのまま落下して自動再挑戦になるか
2. Landing 1から上側のPlanning Landingへ到達できるか
3. 下側のLanding 2を使った近道がなくなっているか
4. Planning Landingから編み枠、生成橋、ゴールまで通して進めるか

### Hook 3下側の補助Hookを削除

- Hook 3の下側にあった`Planning Hook (14.8, 3.0)`を削除する
- 上側の`Hook 3 (13.8, 3.9)`だけを残し、3区間目の接続先を一つに整理する
- Planning Landing、編み枠、生成橋、ゴール、物理設定は変更しない
- Planning LandingからHook 3へ接続し、その後の編む区間まで進めるか確認する

#### プレイ確認結果

- 安全床、下側のLanding 2、Hook 3下側の補助Hookを削除した構成をプレイ確認した
- 制作者本人の評価は「完璧」だった
- 削除した3つの補助要素は戻さず、現在の一続きのルートを採用状態とする

### 4区間のチュートリアルと区間再開を採用

- 第1区間はHook 1にヒモを掛けるだけの安全な練習にする
- 第2区間はHook 2で振り、Landing下の赤い障害物を避けて着地する
- 第3区間は消費して得た編み糸を使い、`Q`で固定足場を生成する
- 第4区間は生成した足場から緑色のゴールへ進む
- Landing 1、Planning Landing、生成した足場への着地を区間チェックポイントにする
- 落下またはプレイ中の`R`では、位置、ヒモ残量、選択長、編み糸、生成足場の状態を区間開始時へ戻す
- クリア後の`R`だけは第1区間からやり直す
- 第1区間を安全にするため、赤い障害物の強制解除対象をHook 1からHook 2へ変更する

#### 確認すること

1. Landing 1へ着地してヒモを外すと第2区間へ進むか
2. 第2区間で赤い障害物へ触れた場合、Landing 1から再開するか
3. Planning Landing到達後の失敗で、保存したヒモ残量と編み糸へ戻るか
4. 足場を編んで着地した後の失敗で、生成済みの足場を保ったまま第4区間から再開するか
5. クリア後に`R`を押すと、全状態を初期化して第1区間へ戻るか

### チュートリアル手前にHookとLandingを1つずつ追加

- 前回は再開処理だけを追加し、ステージ配置がほぼ変わっていなかった
- 意図は、現在のステージの手前に安全な基本区間を実際に増設することだった
- 開始地点を左へ移し、`Tutorial Hook`と`Tutorial Landing`を1つずつ追加する
- 第1区間はTutorial HookからTutorial Landingへ移るだけにする
- 第2区間は既存のHook 1とLanding 1を使い、その下の赤い障害物を避ける
- 第3区間はLanding 1から編む場所へ進み、消費したヒモから足場を生成する
- 第4区間は生成した足場からゴールへ進む
- チェックポイントをTutorial Landing、Landing 1、生成した足場へ割り当て直す

#### 確認すること

1. 画面上でTutorial HookとTutorial Landingが1つずつ増えているか
2. 第1区間では赤い障害物がヒモを強制解除しないか
3. 第2区間ではHook 1接続中に赤い障害物へ触れると失敗するか
4. 第3区間の失敗でLanding 1から再開するか
5. 生成した足場へ着地後、第4区間としてゴールへ進めるか

### 編み糸3本とチュートリアルクリア画面を採用

- Tutorial Hook、Hook 1、Hook 2を正常解除すると、編み糸が合計3本になる
- それまでに使った3回分のヒモをすべて足場へ変えるため、編みに必要な本数を2本から3本へ変更する
- ゴール後は左上HUD内の表示ではなく、画面全体を`TUTORIAL CLEAR`へ切り替える
- クリア画面には「Enter　本編ステージへ」を大きく表示する
- `Enter`を押すと、ビルド順で次にある`MainStage`シーンへ移動する
- `MainStage`は本編設計前の空シーンとし、遷移確認用の案内だけを表示する

#### 確認すること

1. 編み糸が2本では足場を生成できず、3本で`Q`を押すと生成できるか
2. ゴール時に通常HUDが消え、全画面のクリア表示へ切り替わるか
3. クリア画面で`Enter`を押すと`MainStage`へ移動するか
4. `MainStage`で本編ステージの準備中表示が見えるか

#### プレイ確認結果

- 編み糸3本で足場を生成できた
- ゴール時に通常HUDから全画面の`TUTORIAL CLEAR`表示へ切り替わった
- `Enter`で空の本編用`MainStage`へ移動できた
- 制作者本人の評価は「完璧」だった
- このクリア導線を現在の採用状態とする

## 本編ステージを約10区間に分けて作る

- 本編を一気に完成させず、約10区間へ分割して意見交換しながら作る
- 各区間で目的を確認し、実装、プレイ、感想の記録、調整を行ってから次へ進む
- 前半には基本の振り子、短すぎると届かない区間、長すぎると障害物に当たる区間を置く想定
- その後に上下分岐を置き、合流地点を本編の中間地点とする
- 後半では障害物を、適切な長さで直接突破するか、編んだ足場で迂回するかを選べる構成を検討する
- 橋に必要な編み糸の数は先に固定せず、ステージを試した後で調整する
- 明示的な指示があるまで、確認済みのチュートリアルには変更を加えない

### 第1区間：基本の振り子を実装

- 変更対象を`MainStage`だけに限定した
- スタート地面、障害物のない`Main Hook 1`、到達先の`Main Landing 1`を配置した
- 開始時に現在作られている範囲をLanding側からスタート地点へ見せる
- Landingへ上から着地するとHUDに「第1区間 到達」と表示する
- 落下時はヒモ残量と編み糸を初期状態へ戻し、第1区間の最初から再開する
- 数値と配置は仮であり、まず本編の距離感と基本の振り子が適切かをプレイして確認する

#### 第1区間で確認すること

1. 開始時のカメラ移動で、HookとLandingの位置関係を把握できたか
2. 最初のHookへ迷わずヒモを掛けられたか
3. 選んだヒモの長さと、その長さを選んだ理由
4. Landingへの着地が本編の最初として簡単すぎるか、難しすぎるか

#### 第1区間のプレイ確認結果

- 開始時のカメラ移動でHookとLandingの位置関係を把握できた
- 最初のHookへ迷わずヒモを掛けられた
- ヒモの長さを選んでLandingへ着地する流れに問題はなかった
- 第1区間の難易度にも問題はなかった
- 4項目すべて問題なしとして、現在の第1区間を採用する

### 第2区間：歩いて次の振り子を考える

- 第1区間のLandingから、そのまま右へ歩ける短い床を追加する
- 徒歩区間の先に`Main Hook 2`と`Main Landing 2`を配置する
- 徒歩そのものを難しくせず、次のHookとLandingを観察する時間として使う
- プレイヤーが安全な準備位置と、次に使うヒモの長さを考えてから振り子へ移る流れを検証する
- 第2区間にはまだ障害物を置かず、徒歩が「退屈な移動」ではなく「考える間」になるかを確認する
- 確認済みのチュートリアルには変更を加えない

#### 徒歩区間の長さを調整

- Landing 1の着地幅ではなく、その後の徒歩区間を3座標延ばす
- Hook 2とLanding 2も右へ3座標移動し、第2区間の振り子の距離関係は維持する
- 次の配置を観察し、使うヒモの長さを考える時間が増えたかを確認する

#### 徒歩区間のプレイ確認結果

- 延長後の徒歩区間で、Hook 2を見ながら次に使うヒモの長さを考える時間が生まれた
- 単なる移動ではなく、次の振り子へ向けた準備区間として機能した
- 現在の徒歩区間の長さを採用する

#### Hook 2からLanding 2への初回確認

- 選んだヒモの長さは6
- Hook 2へ届く最短の長さが6であり、それが最適だと考えて選んだ
- Landing 2への着地は初回で成功した
- 徒歩中に長さを考える判断は生まれたが、最短の長さがそのまま明確な最適解になっている可能性がある
- 別の長さも選択肢として考える余地があるかは、次の確認で判断する

#### 長さ6を選んだ判断の確認

- 7以上の長さを使うことも候補として考えていた
- 比較したうえで、Hook 2へ届く最短の6が最適だと判断した
- 使える長さを比較し、必要十分な長さを選ぶ計画行動が生まれている
- 第2区間は深い分岐ではなく、次の配置を観察して長さを決める導入区間として現在の形を採用する

### 第3区間：最短と最適をずらす

- Landing 2は、Landing 1とその後の徒歩床を合わせた長さと同程度まで右へ延ばす
- Landing 2の端からHook 3へ掛けられる最短のヒモは6を狙う
- Hook 3からLanding 3へは長さ6や7では届かず、長さ8が必要になる配置を狙う
- 「Hookへ届く最短の長さが、そのまま次のLandingへの最適解とは限らない」と気づく計画行動を検証する
- 配置数値は仮とし、6でHookへ掛けられるか、7で届かないか、8で届くかを順番にプレイして調整する
- 本編完成まではヒモ残量の上限を99にして、区間制作中の資源不足を避ける
- チュートリアルのヒモ残量12と、確認済みの配置は変更しない

#### 第3区間の初回プレイ結果と配置調整

- 初回配置ではHook 3へ掛けられる最短の長さが想定6ではなく7だった
- 長さ8を使ってもLanding 3へ到達できなかった
- 新しい狙いを、Hook 3へ掛けられる最短の長さ5、Landing 3へ到達できる長さ8へ変更する
- Hook 3を左へ2座標移動する
- Landing 3を左へ3座標移動し、Hook 3との距離を1座標縮める
- 高さは維持し、長さ7では届かず8で届く余地が残るかを再確認する

#### 第3区間の再確認結果

- 再調整後は長さ6でLanding 3へ到達できた
- Hook 3へ掛けられる最短と、Landing 3へ届く長さをずらす狙いを満たしていない
- Landing 3を近づけすぎたことが原因と考えられる
- Hook 3の位置は維持し、Landing 3の距離と高さを使って長さ8の軌道だけが届く配置を再調整する

### チュートリアルシーンの名前を整理

- Unity上のシーン名を`Prototype`から`Tutorial`へ変更した
- シーン本体と`.meta`を一緒に移動し、既存の参照GUIDを維持した
- Build Settings、自動生成先、READMEの起動手順を`Tutorial`へ統一した
- 名前だけを変更し、チュートリアルの配置、物理、進行、編み糸3本の設定は変更していない
